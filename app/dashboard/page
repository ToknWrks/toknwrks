export const metadata = {
  title: "Sign In - Open PRO",
  description: "Page description",
};

import Link from "next/link";
import { enableKeplr, getKeplrAccount } from './keplr.api'; // Import the wrapper functions from the same directory

export default function Connect() {
  const handleKeplrConnect = async (event) => {
    event.preventDefault(); // Prevent form submission
    try {
      await enableKeplr();
      const account = await getKeplrAccount();
      if (account) {
        console.log('Connected Keplr Account:', account);
        // Implement your app's logic here (e.g., authenticate, fetch user data, etc.)
      }
    } catch (error) {
      console.error('Keplr Connection Error:', error);
    }
  };

  return (
    <section>
      <div className="mx-auto max-w-6xl px-4 sm:px-6">
        <div className="py-12 md:py-20">
          {/* Section header */}
          <div className="pb-12 text-center">
            <h1 className="animate-[gradient_6s_linear_infinite] bg-[linear-gradient(to_right,theme(colors.gray.200),theme(colors.indigo.200),theme(colors.gray.50),theme(colors.indigo.300),theme(colors.gray.200))] bg-[length:200%_auto] bg-clip-text font-nacelle text-3xl font-semibold text-transparent md:text-4xl">
              Connect to Keplr
            </h1>
          </div>
          {/* Buttons */}
          <div className="mt-6 space-y-5">
            <button
              type="button" // Ensure the button doesn’t submit the form
              className="btn w-full bg-gradient-to-t from-indigo-600 to-indigo-500 bg-[length:100%_100%] bg-[bottom] text-white shadow-[inset_0px_1px_0px_0px_theme(colors.white/.16)] hover:bg-[length:100%_150%]"
              onClick={handleKeplrConnect} 
            > 
              Keplr
            </button>
            <button
              type="button"
              className="btn w-full bg-gradient-to-t from-indigo-600 to-indigo-500 bg-[length:100%_100%] bg-[bottom] text-white shadow-[inset_0px_1px_0px_0px_theme(colors.white/.16)] hover:bg-[length:100%_150%]"
            >
              Rabby
            </button>

            <div className="flex items-center gap-3 text-center text-sm italic text-gray-600 before:h-px before:flex-1 before:bg-gradient-to-r before:from-transparent before:via-gray-400/25 after:h-px after:flex-1 after:bg-gradient-to-r after:from-transparent after:via-gray-400/25">
              {/* Add any additional text or links here */}
            </div>
          </div>
        </div>
      </div>
    </section>
  );
}
